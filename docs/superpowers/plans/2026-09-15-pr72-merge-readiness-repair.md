# PR 72 Merge Readiness Repair Implementation Plan

> **For agentic workers:** Apply the systematic-debugging and test-driven-development workflows task by task.

**Goal:** Repair the evidence-summary gap and obtain a truthful exact-head merge-readiness assessment.
**Architecture:** Preserve all four planes and all remaining blocking checks. Retain the owner-authorized retirement of the redundant Greptile reviewer only in this repository.
**Tech Stack:** Python 3.12, unittest, Git, GitHub rulesets, CodeRabbit.
**Spec:** `AGENTS.md`, `docs/BRANCH_GOVERNANCE.md`, and the owner's PR 72 repair request.

## Global Constraints

- No direct main changes, automatic merge, approval overrides, or weakened validation.
- Greptile must not be reintroduced. No other reviewer is removed or relaxed.
- New code, tests, and developer documentation remain in English.
- Evidence is generated by execution, not inferred from status prose.

## Task 1: Bind unittest gate summaries to captured transcripts

- [ ] Add an adversarial regression in `tests/test_phase2d_evidence_contract.py`; consistently rehash failing or mismatched captures and prove the gate rejects them.
- [ ] Derive the summaries for documentation-language, Phase 2D hardening, and zero-identity-seal gates from their raw transcripts.
- [ ] Run the regression before and after the fix, then validate the full evidence contract.

## Task 2: Refresh execution evidence without changing its schema

- [ ] Run the implementation suite and existing local gates at the new implementation commit.
- [ ] Regenerate the three existing Phase 2D evidence artifacts and run authenticated replay and the final suite.

## Task 3: Correct review metadata and verify readiness

- [ ] Replace accumulated superseded PR closure claims with current, linked evidence and explicit pending review/CI state.
- [ ] Explain the authorized reviewer consolidation, retained blocking coverage, and unchanged Security & CI enforcement; do not claim identical model findings.
- [ ] Request normal CodeRabbit review and pre-merge checks. Confirm the exact HEAD, approval, thread resolution, and required CI before reporting ready. Leave merge to the owner.

## Review follow-up: independently captured queries

Every repository, runner, ruleset, and baseline-canary readback/replay pair must
retain separate process captures in the existing transcript archive. Each capture
records its own execution identifier, start/end timestamps, exact command, exit
status, stdout, stderr, and implementation SHA/tree. Bundle records bind to those
captures by locator and hash. Semantic equality is required without accepting a
copied execution as a second query.

These are operator-collected process transcripts and content-integrity bindings,
not signed GitHub attestations. The opt-in authenticated replay independently
checks current provider state; it does not cryptographically authenticate an old
operator transcript. Baseline-canary captures remain scoped to the historical
run identifiers and must not be presented as a new candidate's runtime execution.

The workflow contract must also reject token references independent of whitespace,
case, property-index notation, or expression wrapping. No workflow permission or
credential grant is added by this regression-test hardening.
